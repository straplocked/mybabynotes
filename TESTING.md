# Trial journal

Live-with-it notes for the testing period. Add entries as they happen — a one-liner beats a forgotten annoyance. When we start iterating, this file becomes the backlog source.

**How to log a note:** add a row. Severity: 💔 blocks/annoys daily · 🤔 friction worth fixing · 💡 idea/nice-to-have · ✅ delighted.

| Date | Who | What happened / what I wanted | Severity |
|---|---|---|---|
| 2026-09-02 | C+K | Cluster feeding skews the feed-rhythm average — Maddux's "every 3h-and-change" window is wrong because back-to-back feeds count as separate rhythm beats. → Fixed: feeds within 45m now group into one session for the average/plan. | 🤔 |
| 2026-09-02 | C+K | We don't track diapers (no health reason to) but the app assumes everyone tracks everything — "0.3 diapers/day" stats are noise. Want per-household on/off for metrics, ideally with the app suggesting it. → Fixed: "What you track" toggles in History + a low-usage nudge; entries are never deleted. | 🤔 |
| 2026-09-02 | C+K | Want wake-window tracking, and the baby's actual DOB so the app tracks age in weeks (the onboarding "2–8 wks" label never advances). → Added: DOB at onboarding + History → About; header age computes live; wake-window avg tile + insight compared against age-typical ranges from docs/feeding-patterns.md. | 💡 |
| 2026-09-05 | C | On duty and there's no checklist — K sees one, I never do, and the whole handoff direction reads backwards. Cause: the plan/checklist only ever existed for whoever *accepted* a handoff. Duty is seeded to the account that created the household and handed straight back by "Hand back", and neither opens a shift — so the person duty keeps returning to sits in a dead state forever, while the person who accepts gets the whole feature. → Fixed: "on duty, nothing started" is now a real state with a start card (drafted plan + "Start my shift") and its own sheet; a pending ask of yours reads "Waiting for {name}" instead of vanishing; "Your shift so far" no longer borrows the other parent's window. | 💔 |
| 2026-09-05 | C | Handoffs read backwards even after the duty fix. Root cause was authorship: `/shifts/request` only ever carried a *note*, and the structured plan was drafted by whoever **accepted**. So the person with the context (who last nursed, what's in the fridge) sent prose, and the person taking over invented the plan. → Fixed: the ask now carries the plan, the "until", an optional recipient, and a note; the receiver adjusts and accepts. "Ask X to take over" became a compose sheet instead of a one-tap send. | 🤔 |
| 2026-09-05 | C | Unfinished plan items died with the shift — a 9am dose nobody got to just vanished when duty changed. → Fixed: non-feed items carry into the next draft at their original (now late) time. Feeds deliberately don't: they're rhythmic, not owed. | 💡 |
| 2026-09-05 | C | Nothing in a plan could be changed — no way to retime an item, drop one, or add anything but another feed. Left over from when the plan was a machine guess the receiver rubber-stamped; once the *asker* authors it, "here's what needs to happen" without a time control is half a feature. → Fixed: every plan row's time is a tap-to-pick control on both sides of a handoff, pending items can be dropped, and "Add to plan" offers any scheduleable tracked type. Logged items freeze. | 🤔 |
| 2026-09-05 | C | Every other entry marks when something *started* — a bottle is stamped at the first sip — but naps landed in the log at the wake-up, so a long nap sorted below feeds that happened during it and the timeline stopped reading top-down. → Fixed in the UI only: rows, day buckets, the shift report, CSV rows and the log sheet's stamp all read `t − duration`. The wire still stamps the wake-up (the timer, the import, the wake-window math and old installed clients all depend on it), and the "since last slept"/"Last nap ended" readouts still measure from the end, which is the point of them. | 🤔 |
| 2026-09-06 | C | The shift card ate the top of Now — on duty, mid-shift, or watching K's, there was always a fat card above the since-cards, and on a phone the actual log started below the fold. → Fixed: the shift surface moved into the sheet behind an icon button in the header, and the "X · on duty" pill shrank to a single icon. An incoming ask still gets an inline card — that one needs answering, not browsing. | 🤔 |
| 2026-09-06 | C | With a nap running, the "Slept" card still read "4h ago" — the last *finished* nap — while the baby was visibly asleep. Same for a nursing timer against "Fed". Two live numbers on one screen disagreeing about the same fact. → Fixed: a card whose type has a running timer flips to that session — "Sleeping now", the stopwatch, "so far" instead of "ago", and whose it is. | 🤔 |
| 2026-09-06 | C | Dark mode read as flat washed-out grey. The background illustrations sat *under* the legibility wash, and since that wash is painted in the backdrop's own colour at up to 85%, dimming the art to 42% brightness underneath left a grey slab where the deep ink should be — the moons, birds and clouds barely showed. → Fixed: the art files carry only their motifs on transparency now (no baked cream field), and in dark the wash drops beneath them so the motifs sit on top of the gradient at 55%. Light keeps the comp's order. Bonus: a non-cream background theme finally tints the illustration instead of being painted over. | 🤔 |
| 2026-09-06 | C | The two new header icons were confusing: an avatar that opened Settings read as a profile switcher, and `swap_horiz` didn't say "shift" — meanwhile History carried its own Settings cog, so Settings had two doors and came back to the wrong one. → Fixed: the avatar became the cog (and History lost its copy), Settings now returns to Now, and the shift button is `pending_actions` — a clipboard of timed items, which is what the sheet actually holds. | 🤔 |
| 2026-09-06 | C | Lifting the motifs out of the wash wasn't enough — dark still looked wrong, and the marketing site was the proof: mybabynotes.app renders the *same* motifs at the *same* 0.6 opacity and reads like night, because it sits on `#161019`. The app was sitting on `#1E1B16` — oklch 0.224 L / 0.011 C against the site's 0.185 / 0.020, i.e. lighter *and* less tinted, which is grey by another name. → Fixed: all five dark backgrounds dropped to the site's depth keeping their own hue (cream is now `#161019` verbatim), `--frame` to `#0A070C`, and the dark `theme-color` + manifest `color_scheme_dark` moved with them. The site leads on dark from here. | 🤔 |
| 2026-09-06 | C | The hand-off drawer didn't grab. The log sheet has had drag-to-dismiss for a while — handle *or* a pull-down on the content — so reaching for the same flick on the shift sheet and getting nothing read as the app being broken, not as a missing feature. Its handle was a decorative pill with no listeners, and it scrolled away with the plan. → Fixed: both drawers now run one gesture (`sheetGestures`), so the thresholds can't drift apart, and the shift sheet got the log sheet's structure — a fixed handle strip over a scrolling body. It has one height, so an up-drag rubber-bands instead of expanding. | 🤔 |
| 2026-09-06 | C | Opened the log sheet on the phone after updating and it was a flat light-grey slab — dark cards and cream text sitting on it, unreadable. Not a palette bug: the sheet measured (158,154,154), which is exactly the *old* opaque-cream art at the new 0.6 opacity over `#161019`. `/art/*.png` lived in `public/`, i.e. outside the hashed `/assets/` bundle, so the service worker's stale-while-revalidate handed the new stylesheet a bitmap drawn for the old one. `app-bg.png` had already revalidated; `sheet-bg.png` hadn't, because it only loads when the sheet first opens. → Fixed: the art moved to `src/art/` and is imported, so it's content-hashed and can't mismatch the build (and no longer 404s under HA ingress); `sw.js` went to v5 so caches already holding the old bitmaps purge instead of aging out. | 💔 |
| 2026-09-06 | C | "What's the difference between hand back and hand off? I seem to have BOTH." Two buttons, same recipient, no clue that one moves duty instantly and the other waits for a yes — and on a shift I'd started myself the big one said "Hand back to K", which would have dumped duty on K without asking. The footer shortcut made it worse: it said "Hand off" but opened a sheet whose main button said "Hand back". → Fixed: hand-back only exists when someone actually handed you the shift; otherwise asking is the only (and primary) action. Verbs now carry the difference — "Hand back to K now" vs "Ask K to take over" — with a line under them saying which waits. The footer shortcut is gone; the header button is the one way in. | 💔 |
| 2026-09-06 | C | Logging a *missed* feed in the middle of the night is where the sheet runs out. The nudges stop at −1h and the time picker only reaches back through last night (a time later than now = yesterday), so anything from an earlier day simply couldn't be entered — and the day is exactly what you're most likely to be reconstructing at 3am. → Fixed: an "Advanced" link, right-justified on the Cancel row, folds out a day control (Today / Yesterday / a calendar capped at today) plus a plain reading of what will be saved — `Sat, Sep 5 · 11:40 PM`, with `→ 12:25 AM` on a nap, whose end the sheet never printed before. Folded away by default because the common path is "now"; folded *open* when you edit an entry that isn't from today, and absent on the timer path. The stamp's kicker now says "Yesterday" / "5 days ago" instead of a bare time that looks like today's. | 🤔 |
| 2026-09-06 | C | Wanted the day control's fold state to be a choice, not a decision made for me — if you're the one backfilling, that extra tap every time is the annoyance the drawer was meant to remove. → Added: "Advanced log options" in Settings → Appearance, **off by default** (most logs really are "now"), device-local like theme, tilt and timer placement — one parent can have the day picker always out without changing the other's sheet. The Appearance footer stopped enumerating which prefs stay on the phone; a list that goes stale reads as "the rest of these sync". | 💡 |
| 2026-09-07 | K | As parents we should be able to stop a timer whoever started it. A nap started on one phone could only be ended on that phone, so the person coming on duty was stuck watching a stopwatch they couldn't stop — exactly at the handoff, which is when the timer is most likely to outlive its starter. → Fixed: Stop now shows on every running timer on both surfaces (top cards and Today rows). The session stays credited to whoever ran it rather than whoever pressed Stop — entries can now name their author (`user_id` on `POST /entries`, honored only for members of your own household and only on create), so a feed one parent ends is still the other parent's feed. Remaining edge in known-limitations: the nurse Left/Right pick is remembered per device, so stopping someone else's nursing timer falls back to the default side. | 💔 |
| | | | |

## Questions worth answering while we use it

Seeded from design intent + known soft spots — jot verdicts inline.

### The 3-tap promise
- Is *open → + → Save* actually the common case, or do you usually override the prediction?
- How often is smart prefill **right** (type + amount/side)? When it's wrong, is it wrong in a predictable way?
- Are the time nudges (−5/−15/−1h) the right increments for real backfilling?
- The day now lives behind "Advanced" on the Cancel row — is that findable the night you need it, or does the link need to be louder (or the day chips promoted onto the stamp row)? How often does a backfill actually cross midnight?
- PWA from the home screen: cold-start speed at 3am? Does it open to Home logged-in every time?

### Both of you, one log
- Did partner's entries ever feel stale or missing? (Realtime should be ~instant with the app open; note any lag + whether on LAN or cellular.)
- Any duplicate or lost entries after offline stretches (elevator, airplane mode, dead zones)?
- Invite flow: was sharing the code manually awkward enough to justify real emails?

### Shifts
- Does request → accept → hand back match how you two actually trade off, or is instant handoff ("you have him now") the real pattern? (Partly answered 2026-09-05: neither — the common case is *nobody asked*, one of you just has the baby. Hence the start card. Still open: should handing back auto-start a shift for whoever receives it, or is one tap right?)
- Should there be a way to say "K is usually the one with him" so duty defaults to the right person after a reset?
- With the duty avatar gone, do you ever actually need "who has him" at a glance on Now, or is the footer verb ("Hand off" vs "Take over") enough?
- Now the shift lives behind a header button: does the running plan get *checked* as often as when it sat on Now, or does out-of-sight mean out-of-mind? Is the pulsing dot on an incoming ask loud enough on a locked-then-opened phone?
- **The ask is heavier now** (plan + window + note) on the bet that it went unused *because* it carried nothing. Watch whether that's right: does composing a handoff actually happen, or does the extra step push you further toward one of you just taking the baby? It's built to be two taps if you accept the defaults — is it?
- Does the receiver ever *change* the plan they're sent, or is it always accepted as-is? (If always as-is, the toggles are ceremony and the carer case wants the plan locked instead.)
- Now that plan times are editable: do you actually retime items, or is the rhythm's guess always close enough? And is tapping the time discoverable, or does the pencil need to be louder?
- Does a carried-forward dose actually get done, or does it just accumulate as a late red row that gets ignored?
- Is the auto-drafted plan (next feeds from rhythm + meds) believable? Right number of items?
- Did you miss a handoff request because the app was closed? (→ push notifications priority)
- Is the shift report card the right summary, or do you want different rows?

### Home & History
- A running timer now takes over its since-card ("Sleeping now · 42:10 so far"). With timers also shown as top cards and Today rows, is that a third copy too many — should the top timer card go away when the since-card already says it?
- The since-cards are now picked per household from seven (fed, pumped, diaper, slept, tummy time, bath, meds) — which did you actually keep, and is the picker discoverable?
- Is 12 timeline entries enough per day? Do you reach for "older days" and hit the 7-day wall?
- Naps now sit at their start time, but the "Slept" since-card still counts from the **wake-up** (that's the wake window, and it's the number you act on). Does the card showing 6:00 AM next to a row showing 3:00 AM read as two facts or as a bug? Same question for "Last nap ended" in a shift report.
- Is the feeds-rhythm insight ("roughly every 3h 23m") useful or noise?

### Feel
- Anything that felt slow, janky, or mis-tapped on a real phone (tap targets, sheet height, keyboard overlap on inputs)?
- Does the peach/plum branding hold up on-device? Icon/splash on the home screen okay?
- Set the app to another language for a day — does anything overflow, read wrong, or stay stubbornly English where it shouldn't?

## Iteration parking lot

Bigger ideas that surfaced — no commitment implied. See also [docs/known-limitations.md](docs/known-limitations.md) for the remaining known gaps (no custom entry types or per-entry notes, no month view, household-level shifts and meds nudge, unreviewed translations…).

- 
